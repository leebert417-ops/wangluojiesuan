# UITable 清空数据功能使用说明

## 📦 文件信息

**文件名**：`clear_uitable.m`
**位置**：`General Problem Solver/+gps/+ui/`
**版本**：v1.0 (2025-12-18)
**作者**：MATLAB 通风工程专家助手

---

## 🎯 功能说明

清空 App Designer 的 UITable 中的所有数据，支持以下功能：

- ✅ **一键清空所有行**
- ✅ **保留表格列结构**（清空后仍是 table 类型）
- ✅ **可选确认对话框**（默认显示，防止误操作）
- ✅ **自定义确认消息**
- ✅ **边界情况处理**（空表格）
- ✅ **返回操作状态**（成功/失败）

---

## 🚀 快速使用

### 基本用法（推荐，带确认）

```matlab
% "清空"按钮回调函数
function ClearButtonPushed(app, event)
    % 带确认对话框的清空（防止误操作）
    gps.ui.clear_uitable(app.UITable, 'confirm', true);
end
```

### 不带确认对话框

```matlab
function ClearButtonPushed(app, event)
    % 直接清空，不显示确认对话框
    gps.ui.clear_uitable(app.UITable, 'confirm', false);
end
```

### 获取操作结果

```matlab
function ClearButtonPushed(app, event)
    % 返回是否成功清空
    success = gps.ui.clear_uitable(app.UITable, 'confirm', true);

    if success
        app.StatusLabel.Text = '表格已清空';
        app.StatusLabel.FontColor = [0 0.6 0];  % 绿色
    end
end
```

---

## 📋 参数说明

### 输入参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `uitableHandle` | `matlab.ui.control.Table` | 必需 | UITable 组件句柄 |
| `confirm` | `logical` | `true` | 是否显示确认对话框 |
| `confirmMsg` | `string` | 自动生成 | 自定义确认消息 |

### 输出参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `success` | `logical` | 是否成功清空（true/false）|

---

## 🎨 使用场景示例

### 场景 1：基本清空（带确认）

```matlab
function ClearButtonPushed(app, event)
    gps.ui.clear_uitable(app.UITable, 'confirm', true);
end
```

### 场景 2：清空后更新状态

```matlab
function ClearButtonPushed(app, event)
    % 清空表格
    success = gps.ui.clear_uitable(app.UITable, 'confirm', true);

    % 更新状态标签
    if success
        app.StatusLabel.Text = '表格已清空';
        app.TotalRowsLabel.Text = '共 0 行';
    end
end
```

### 场景 3：自定义确认消息

```matlab
function ClearButtonPushed(app, event)
    % 自定义确认消息
    gps.ui.clear_uitable(app.UITable, ...
        'confirm', true, ...
        'confirmMsg', '清空后将无法恢复，确定要删除所有数据吗？');
end
```

### 场景 4：清空前检查

```matlab
function ClearButtonPushed(app, event)
    % 检查表格是否为空
    if isempty(app.UITable.Data) || height(app.UITable.Data) == 0
        uialert(app.UIFigure, '表格已经为空', '提示');
        return;
    end

    % 执行清空
    success = gps.ui.clear_uitable(app.UITable, 'confirm', true);

    if success
        % 清空后的额外操作
        app.ExportButton.Enable = 'off';  % 禁用导出按钮
        app.DeleteButton.Enable = 'off';  % 禁用删除按钮
    end
end
```

### 场景 5：清空并重新加载

```matlab
function ResetButtonPushed(app, event)
    % 清空当前数据
    success = gps.ui.clear_uitable(app.UITable, 'confirm', true);

    if success
        % 重新加载默认数据
        defaultFile = 'data/branches_default.csv';
        if isfile(defaultFile)
            gps.ui.import_branches_csv_to_uitable(app.UITable, defaultFile);
            uialert(app.UIFigure, '已重置为默认数据', '完成', 'Icon', 'success');
        end
    end
end
```

### 场景 6：快捷键清空

```matlab
function UIFigureKeyPress(app, event)
    % Ctrl+Shift+D：清空表格
    if strcmp(event.Key, 'd') && ...
       ismember('control', event.Modifier) && ...
       ismember('shift', event.Modifier)
        gps.ui.clear_uitable(app.UITable, 'confirm', true);
    end
end
```

---

## 📊 操作示例

### 操作前

| ID | 起点 | 终点 | 风阻 |
|----|------|------|------|
| 1 | 1 | 2 | 0.1 |
| 2 | 2 | 3 | 0.2 |
| 3 | 3 | 4 | 0.15 |

**执行**：`gps.ui.clear_uitable(app.UITable, 'confirm', true)`

### 确认对话框

```
┌──────────────────────────────────┐
│  确认清空                        │
├──────────────────────────────────┤
│  确定要清空表格中的所有数据吗？  │
│                                  │
│  当前共有 3 行数据。             │
│  此操作不可撤销！                │
│                                  │
│          [清空]  [取消]          │
└──────────────────────────────────┘
```

### 操作后

| ID | 起点 | 终点 | 风阻 |
|----|------|------|------|
| （空） | （空） | （空） | （空） |

**结果**：表格为空，但列结构保留（仍是 4 列的 table）

---

## 🔧 在 App Designer 中配置

### 步骤 1：添加"清空"按钮

1. 打开 App Designer
2. 从组件库拖入 `Button` 组件
3. 设置属性：
   - **Text**: "清空表格" 或 "清空"
   - **Tag**: "ClearButton"
   - **Icon**: 可选择删除图标
   - **Position**: 在表格附近合适位置

### 步骤 2：绑定回调函数

**方法 A：可视化绑定**
1. 右键点击按钮 → "回调" → "ButtonPushedFcn"
2. 在生成的回调中添加代码：

```matlab
function ClearButtonPushed(app, event)
    gps.ui.clear_uitable(app.UITable, 'confirm', true);
end
```

**方法 B：代码绑定**
```matlab
function startupFcn(app)
    app.ClearButton.ButtonPushedFcn = @(src, event) ...
        gps.ui.clear_uitable(app.UITable, 'confirm', true);
end
```

### 步骤 3：测试

1. 保存并运行 App
2. 在表格中添加一些数据
3. 点击"清空"按钮
4. 验证：
   - ✅ 弹出确认对话框
   - ✅ 确认后成功清空所有数据
   - ✅ 表格列结构保留

---

## 🧪 测试验证

### 运行完整测试

```matlab
% 在 MATLAB 命令窗口执行
cd 'General Problem Solver/+gps/+ui'
test_clear_uitable
```

### 测试覆盖清单

- ✅ **测试 1**：清空有数据的表格（不带确认）
- ✅ **测试 2**：清空空表格
- ✅ **测试 3**：验证表格结构保留
- ✅ **测试 4**：清空后重新添加数据
- ✅ **测试 5**：用户交互演示（带确认）

---

## 📈 配套功能组合

### 完整的表格管理工作流

```
┌──────────┐
│ 导入 CSV │
└────┬─────┘
     │
     ▼
┌──────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│ 表格显示 │◄────▶│ 添加新行 │◄────▶│ 删除选中 │◄────▶│ 清空表格 │ ← 本功能
└────┬─────┘      └──────────┘      └──────────┘      └──────────┘
     │
     ▼
┌──────────┐
│ 导出 CSV │
└──────────┘
```

### 推荐按钮布局

```
┌───────────────────────────────────────────────────────┐
│ [导入]  [导出]  [添加新行]  [删除选中]  [清空]      │
├───────────────────────────────────────────────────────┤
│                   UITable                             │
└───────────────────────────────────────────────────────┘
```

---

## 💡 高级用法

### 用法 1：清空后自动禁用按钮

```matlab
function ClearButtonPushed(app, event)
    % 清空表格
    success = gps.ui.clear_uitable(app.UITable, 'confirm', true);

    if success
        % 禁用依赖数据的按钮
        app.ExportButton.Enable = 'off';
        app.DeleteButton.Enable = 'off';
        app.SolveButton.Enable = 'off';

        % 启用导入按钮
        app.ImportButton.Enable = 'on';
    end
end
```

### 用法 2：清空并保存备份

```matlab
function ClearButtonPushed(app, event)
    % 保存清空前的数据（用于撤销）
    app.BackupData = app.UITable.Data;

    % 清空表格
    success = gps.ui.clear_uitable(app.UITable, 'confirm', true);

    if success
        % 启用恢复按钮
        app.UndoButton.Enable = 'on';
    end
end

function UndoButtonPushed(app, event)
    % 恢复清空前的数据
    if ~isempty(app.BackupData)
        app.UITable.Data = app.BackupData;
        app.UndoButton.Enable = 'off';
        uialert(app.UIFigure, '已恢复数据', '完成', 'Icon', 'success');
    end
end
```

### 用法 3：右键菜单清空

```matlab
function startupFcn(app)
    % 创建右键菜单
    cm = uicontextmenu(app.UIFigure);
    app.UITable.ContextMenu = cm;

    % 添加清空菜单项
    uimenu(cm, 'Text', '清空所有数据', 'MenuSelectedFcn', ...
        @(src, event) gps.ui.clear_uitable(app.UITable, 'confirm', true));

    % 添加分隔符
    uimenu(cm, 'Separator', 'on');
end
```

---

## ⚠️ 注意事项

### 1. 确认对话框的重要性

- **强烈建议**使用 `confirm = true`（默认值）
- 清空操作**不可撤销**，除非自己实现备份机制
- 对于重要数据，建议在清空前导出备份

### 2. 表格结构保留

- 清空后，表格仍保持 `table` 类型
- 列名和列数不变
- 可以立即添加新数据

### 3. 与删除功能的区别

| 功能 | 清空 | 删除选中行 |
|-----|------|-----------|
| **操作对象** | 所有行 | 选中的行 |
| **确认对话框** | 默认显示 | 可选 |
| **ID 重排** | N/A（已无数据） | 可选（默认启用）|
| **使用场景** | 重置表格 | 删除特定行 |

### 4. 清空后的状态

```matlab
% 清空后
height(app.UITable.Data)  % 返回 0
isempty(app.UITable.Data)  % 返回 true (如果是空 table)
istable(app.UITable.Data)  % 返回 true (仍是 table 类型)
```

---

## 🔗 相关功能

| 功能 | 函数 | 说明 |
|-----|------|------|
| 导入数据 | `import_branches_csv_to_uitable.m` | 从 CSV 导入到表格 |
| 导出数据 | `export_uitable_to_branches_csv.m` | 从表格导出到 CSV |
| 添加新行 | `add_new_row_to_uitable.m` | 在表格末尾添加行 |
| 删除选中行 | `delete_selected_rows_from_uitable.m` | 删除选中的行 |
| **清空表格** | **`clear_uitable.m`** | **清空所有数据** |

---

## 📝 常见问题

### Q1：清空后能否撤销？

**A**：当前版本不支持自动撤销。建议：
- 清空前提示用户导出备份
- 或自己实现备份机制（见"高级用法 2"）

### Q2：清空和删除所有行有什么区别？

**A**：
- **清空**：一键删除所有行，更快捷，专门用于重置表格
- **删除所有行**：需要先选中所有行，然后删除，更适合删除部分行

### Q3：清空后表格还能使用吗？

**A**：可以。清空只是删除数据，表格结构保留，可以：
- 重新导入数据
- 手动添加新行
- 正常使用所有功能

### Q4：能否跳过确认对话框？

**A**：可以，使用 `'confirm', false`：
```matlab
gps.ui.clear_uitable(app.UITable, 'confirm', false);
```
但**不推荐**用于生产环境（容易误操作）。

---

## 📈 版本历史

- **v1.0** (2025-12-18) - 初始版本
  - 一键清空所有数据
  - 可选确认对话框
  - 表格结构保留
  - 完整的错误处理

---

## 👤 作者

MATLAB 通风工程专家助手

## 📄 许可

遵循项目主许可证
