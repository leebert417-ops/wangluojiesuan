# 通风网络求解器 UI 接口使用说明

## 📦 文件信息

**文件名**：`solve_network_from_ui.m`
**位置**：`General Problem Solver/+gps/+ui/`
**版本**：v1.0 (2025-12-18)
**作者**：MATLAB 通风工程专家助手

---

## 🎯 功能说明

从 App Designer 的 UI 组件中获取所有求解参数，并调用通风网络求解器（Hardy Cross 方法）。

**主要功能**：
- ✅ 从 UITable 获取分支数据（ID、起点、终点、风阻）
- ✅ 从 EditField 获取边界条件（总风量、入/回风节点）
- ✅ 从多个组件获取求解器选项（迭代数、容差、松弛因子等）
- ✅ **10+ 项数据验证**（ID 连续性、节点有效性、自环检测等）
- ✅ 自动调用求解器并显示结果
- ✅ 友好的错误提示和成功消息

---

## 🚀 快速使用

### 在 App Designer 按钮回调中使用

```matlab
% "求解"按钮的回调函数
function SolveButtonPushed(app, event)
    % 调用求解函数
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);

    if success
        % 求解成功，可选：显示结果到表格或图表
        app.ResultsTextArea.Value = sprintf('求解成功！迭代次数：%d', Results.iterations);
    end
end
```

---

## 📋 必需的 UI 组件

### 1. 数据输入组件

| 组件名 | 类型 | 用途 | 示例值 |
|--------|------|------|--------|
| `app.UITable` | `Table` | 分支数据表格 | ID=1, 起点=1, 终点=2, 风阻=0.1 |

**UITable 列结构**（必须包含 4 列）：
- 第 1 列：ID / 编号（整数，1~B 连续）
- 第 2 列：起点 / from_node（整数，>= 1）
- 第 3 列：终点 / to_node（整数，>= 1）
- 第 4 列：风阻 / R（正数）

### 2. 边界条件组件

| 组件名 | 类型 | 用途 | 示例值 |
|--------|------|------|--------|
| `app.EditField` | `Numeric Edit Field` | 初始风量（系统总风量，m³/s） | `100` |
| `app.EditField_2` | `Edit Field (Text)` | 入风节点（逗号分隔） | `"1"` 或 `"1,2"` |
| `app.EditField_3` | `Edit Field (Text)` | 回风节点（逗号分隔） | `"10"` 或 `"9,10"` |

### 3. 求解器选项组件

| 组件名 | 类型 | 用途 | 默认值 |
|--------|------|------|--------|
| `app.EditField_4` | `Numeric Edit Field` | 最大迭代数 | `1000` |
| `app.EditField_5` | `Numeric Edit Field` | 收敛容差 | `0.001` (1e-3) |
| `app.Slider` | `Slider` | 松弛因子（0~2） | `1.0` |
| `app.DropDown` | `Drop Down` | 信息显示 | `'显示'` 或 `'隐藏'` |
| `app.DropDown_2` | `Drop Down` | 求解方法 | `'Hardy Cross'` |

### 4. 可选组件

| 组件名 | 类型 | 用途 |
|--------|------|------|
| `app.UIFigure` | `UI Figure` | 主窗口（用于弹出对话框） |

---

## 📊 数据验证清单

函数会自动执行以下验证：

### 分支数据验证（UITable）
1. ✅ 表格非空
2. ✅ 表格为 `table` 类型
3. ✅ 至少包含 4 列
4. ✅ 至少有 1 行数据
5. ✅ ID 为 1~B 的连续编号（无重复、无缺失）
6. ✅ 节点编号 >= 1
7. ✅ 节点编号为有限整数
8. ✅ 无自环（起点 ≠ 终点）
9. ✅ 风阻为有限正数

### 边界条件验证
10. ✅ 总风量为正数
11. ✅ 入风节点格式正确（逗号分隔的整数）
12. ✅ 回风节点格式正确
13. ✅ 入/回风节点不重叠
14. ✅ 入/回风节点在网络范围内

### 求解器选项验证
15. ✅ 最大迭代数为正整数
16. ✅ 收敛容差为正数
17. ✅ 松弛因子在 (0, 2] 范围内

---

## 🎨 使用场景示例

### 场景 1：基本求解

```matlab
function SolveButtonPushed(app, event)
    % 直接求解
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);
end
```

### 场景 2：求解后显示结果到表格

```matlab
function SolveButtonPushed(app, event)
    % 求解
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);

    if success
        % 将风量结果添加到 UITable 的新列
        app.UITable.Data.风量 = Q;

        % 添加压降列
        app.UITable.Data.压降 = Results.pressure_diff_signed;
    end
end
```

### 场景 3：求解后绘制收敛曲线

```matlab
function SolveButtonPushed(app, event)
    % 求解
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);

    if success
        % 绘制收敛曲线
        figure;
        semilogy(1:Results.iterations, Results.residual_history);
        xlabel('迭代次数');
        ylabel('最大回路残差');
        title('Hardy Cross 收敛曲线');
        grid on;
    end
end
```

### 场景 4：求解前确认

```matlab
function SolveButtonPushed(app, event)
    % 确认对话框
    choice = uiconfirm(app.UIFigure, ...
        '确定要开始求解吗？', ...
        '确认', ...
        'Options', {'求解', '取消'});

    if strcmp(choice, '求解')
        [Q, Results, success] = gps.ui.solve_network_from_ui(app);
    end
end
```

### 场景 5：求解后保存结果

```matlab
function SolveButtonPushed(app, event)
    % 求解
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);

    if success
        % 保存结果到 MAT 文件
        [file, path] = uiputfile('results.mat', '保存求解结果');
        if file ~= 0
            save(fullfile(path, file), 'Q', 'Results');
            uialert(app.UIFigure, '结果已保存', '完成', 'Icon', 'success');
        end
    end
end
```

### 场景 6：禁用求解按钮（防止重复求解）

```matlab
function SolveButtonPushed(app, event)
    % 禁用求解按钮
    app.SolveButton.Enable = 'off';
    app.SolveButton.Text = '求解中...';

    try
        % 求解
        [Q, Results, success] = gps.ui.solve_network_from_ui(app);

    catch ME
        % 错误处理
        uialert(app.UIFigure, ME.message, '错误');
    end

    % 恢复按钮
    app.SolveButton.Enable = 'on';
    app.SolveButton.Text = '求解';
end
```

---

## 🔧 在 App Designer 中配置

### 步骤 1：创建必需的 UI 组件

#### 分支数据表格
1. 拖入 `Table` 组件，命名为 `UITable`
2. 设置列名：`{'ID', '起点', '终点', '风阻'}`
3. 设置列可编辑性：`[false, true, true, true]`

#### 边界条件输入框
1. 拖入 `Numeric Edit Field`，命名为 `EditField`，标签为"初始风量"
2. 拖入 `Edit Field (Text)`，命名为 `EditField_2`，标签为"入风节点"
3. 拖入 `Edit Field (Text)`，命名为 `EditField_3`，标签为"回风节点"

#### 求解器选项
1. 拖入 `Numeric Edit Field`，命名为 `EditField_4`，标签为"最大迭代数"，默认值=1000
2. 拖入 `Numeric Edit Field`，命名为 `EditField_5`，标签为"收敛容差"，默认值=0.001
3. 拖入 `Slider`，命名为 `Slider`，标签为"松弛因子"，范围=[0, 2]，默认值=1.0
4. 拖入 `Drop Down`，命名为 `DropDown`，标签为"信息显示"，选项={'显示', '隐藏'}
5. 拖入 `Drop Down`，命名为 `DropDown_2`，标签为"求解方法"，选项={'Hardy Cross'}

### 步骤 2：创建"求解"按钮

1. 拖入 `Button` 组件
2. 设置属性：
   - **Text**: "求解"
   - **Tag**: "SolveButton"
   - **Icon**: 可选择齿轮图标

### 步骤 3：绑定回调函数

右键点击"求解"按钮 → 回调 → ButtonPushedFcn：

```matlab
function SolveButtonPushed(app, event)
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);
end
```

### 步骤 4：设置默认值（可选）

在 `startupFcn` 中设置默认值：

```matlab
function startupFcn(app)
    % 添加路径
    addpath('General Problem Solver');

    % 设置默认值
    app.EditField.Value = 100;           % 初始风量
    app.EditField_2.Value = '1';         % 入风节点
    app.EditField_3.Value = '10';        % 回风节点
    app.EditField_4.Value = 1000;        % 最大迭代数
    app.EditField_5.Value = 0.001;       % 收敛容差
    app.Slider.Value = 1.0;              % 松弛因子
    app.DropDown.Value = '显示';         % 信息显示
    app.DropDown_2.Value = 'Hardy Cross'; % 求解方法
end
```

---

## 📊 输入/输出说明

### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `app` | App Designer 对象 | 包含所有必需UI组件的应用对象 |

### 输出参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `Q` | `double` (B×1) | 各分支风量（m³/s），正值表示正向流动 |
| `Results` | `struct` | 求解结果结构体（详见下表） |
| `success` | `logical` | 是否求解成功 |

### Results 结构体字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `iterations` | 整数 | 实际迭代次数 |
| `converged` | logical | 是否收敛 |
| `max_residual` | double | 最大回路残差 |
| `residual_history` | double 向量 | 每次迭代的残差历史 |
| `pressure_diff_signed` | double (B×1) | 各分支压降（带符号） |
| `pressure_diff_abs` | double (B×1) | 各分支压降（绝对值） |
| `node_residual` | double (N×1) | 各节点流量守恒残差 |
| `LoopMatrix` | double (M×B) | 回路-分支关联矩阵 |
| `network_info` | struct | 网络信息（N, B, M） |

---

## ⚠️ 常见错误及解决方法

### 错误 1：UITable 为空

**错误信息**：`UITable 为空，请先导入或添加分支数据`

**解决方法**：
- 点击"导入"按钮导入 CSV 文件
- 或点击"添加新行"按钮手动添加数据

### 错误 2：分支 ID 不连续

**错误信息**：`分支 ID 必须为 1~8 的连续编号（当前缺失或重复）`

**解决方法**：
- 删除行后使用 `'reindexID', true` 重排 ID
- 或手动修正 ID 列为连续编号

### 错误 3：节点编号格式错误

**错误信息**：`入风节点格式错误（当前值：1,2,3）`

**解决方法**：
- 确保使用英文逗号分隔
- 示例：`"1"` 或 `"1,2"`

### 错误 4：入/回风节点重叠

**错误信息**：`入风节点与回风节点不能重叠`

**解决方法**：
- 检查入风节点和回风节点是否有相同编号
- 修改为不同的节点

### 错误 5：未收敛

**警告信息**：`求解未完全收敛（迭代次数：1000，最大残差：0.05）`

**解决方法**：
- 增加最大迭代数（如 2000）
- 调整松弛因子（尝试 0.5~1.5）
- 检查网络数据是否合理

---

## 💡 高级技巧

### 技巧 1：批量求解（参数扫描）

```matlab
% 扫描不同的总风量
Q_values = 50:10:150;
results_all = cell(length(Q_values), 1);

for i = 1:length(Q_values)
    app.EditField.Value = Q_values(i);
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);
    results_all{i} = Results;
end
```

### 技巧 2：实时显示求解进度

在求解前设置状态标签：

```matlab
app.StatusLabel.Text = '正在求解...';
drawnow;

[Q, Results, success] = gps.ui.solve_network_from_ui(app);

app.StatusLabel.Text = sprintf('求解完成（迭代：%d）', Results.iterations);
```

### 技巧 3：结果可视化

```matlab
if success
    % 绘制网络图，边的粗细表示风量大小
    figure;
    Branches = app.UITable.Data;
    G = graph(Branches.起点, Branches.终点, abs(Q));
    plot(G, 'LineWidth', G.Edges.Weight/max(abs(Q))*5);
    title('通风网络风量分布');
end
```

---

## 🔗 相关功能

| 功能 | 函数 | 说明 |
|-----|------|------|
| 导入分支数据 | `import_branches_csv_to_uitable.m` | 从 CSV 导入到 UITable |
| 导出结果 | `export_uitable_to_branches_csv.m` | 导出 UITable（含风量） |
| 核心求解器 | `ventilation_network_solver_generic.m` | Hardy Cross 求解器 |
| **UI 求解接口** | **`solve_network_from_ui.m`** | **从 UI 获取参数并求解** |

---

## 📝 版本历史

- **v1.0** (2025-12-18) - 初始版本
  - 完整的 UI 数据提取
  - 10+ 项数据验证
  - 自动错误提示
  - 友好的结果显示

---

## 👤 作者

MATLAB 通风工程专家助手

## 📄 许可

遵循项目主许可证
