# GPS UI 模块功能总览

## 📦 模块概述

**位置**：`General Problem Solver/+gps/+ui/`
**版本**：v1.0 (2025-12-18)
**作者**：MATLAB 通风工程专家助手

本模块提供了一整套用于 App Designer 的 UITable 操作函数，用于通风网络数据的导入、编辑、导出。

---

## 🎯 功能列表

| 功能 | 函数文件 | 大小 | 状态 | 说明 |
|-----|---------|------|------|------|
| **CSV 导入** | `import_branches_csv.m` | 3.5 KB | ✅ | 读取 CSV 文件（支持注释行）|
| **导入到表格** | `import_branches_csv_to_uitable.m` | 3.4 KB | ✅ | 将 CSV 数据填充到 UITable |
| **导出到 CSV** | `export_uitable_to_branches_csv.m` | 8.5 KB | ✅ | 将 UITable 数据导出为 GPS 格式 CSV |
| **添加新行** | `add_new_row_to_uitable.m` | 3.7 KB | ✅ | 在表格末尾添加新行（ID 自动递增）|
| **删除选中行** | `delete_selected_rows_from_uitable.m` | 4.7 KB | ✅ | 删除选中的行（支持 ID 重排，默认启用）|
| **清空表格** | `clear_uitable.m` | 4.2 KB | ✅ | 清空所有数据（带确认对话框）|
| **求解网络** | `solve_network_from_ui.m` | 10.5 KB | ✅ | 从 UI 获取参数并调用求解器（Hardy Cross）|

---

## 📚 文档索引

- [导出功能文档](README_export.md) - 导出 UITable 到 CSV
- [添加行功能文档](README_add_row.md) - 在表格末尾添加新行
- [删除行功能文档](README_delete_row.md) - 删除选中的行
- [清空功能文档](README_clear.md) - 清空所有数据
- [求解功能文档](README_solve.md) - 从 UI 获取参数并求解网络

---

## 🚀 快速上手

### 1. 导入数据

```matlab
% 导入按钮回调
function ImportButtonPushed(app, event)
    T = gps.ui.import_branches_csv_to_uitable(app.UITable);
end
```

### 2. 添加新行

```matlab
% 添加新行按钮回调
function AddRowButtonPushed(app, event)
    gps.ui.add_new_row_to_uitable(app.UITable);
end
```

### 3. 删除选中行

```matlab
% 删除行按钮回调（默认重排 ID）
function DeleteRowButtonPushed(app, event)
    gps.ui.delete_selected_rows_from_uitable(app.UITable, 'confirm', true);
end

% 如需保留原 ID，使用：
% gps.ui.delete_selected_rows_from_uitable(app.UITable, 'reindexID', false, 'confirm', true);
```

### 4. 清空表格

```matlab
% 清空按钮回调（带确认对话框）
function ClearButtonPushed(app, event)
    gps.ui.clear_uitable(app.UITable, 'confirm', true);
end
```

### 5. 求解网络

```matlab
% 求解按钮回调
function SolveButtonPushed(app, event)
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);

    if success
        % 将风量结果添加到表格
        app.UITable.Data.风量 = Q;
    end
end
```

### 6. 导出数据

```matlab
% 导出按钮回调
function ExportButtonPushed(app, event)
    gps.ui.export_uitable_to_branches_csv(app.UITable);
end
```

---

## 🎨 完整工作流

```
┌──────────┐
│ CSV 文件 │
└─────┬────┘
      │
      │ import_branches_csv_to_uitable.m
      ▼
┌─────────────────────────────────────┐
│          UITable (表格)             │
│  ┌────┬──────┬──────┬────────┐     │
│  │ ID │ 起点 │ 终点 │ 风阻   │     │
│  ├────┼──────┼──────┼────────┤     │
│  │  1 │   1  │   2  │ 0.1    │     │
│  │  2 │   2  │   3  │ 0.2    │     │
│  │ ...│  ... │  ... │ ...    │     │
│  └────┴──────┴──────┴────────┘     │
└─────────────────────────────────────┘
      │           │           │      │
      │           │           │      │
      ▼           ▼           ▼      ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 添加新行 │ │ 手动编辑 │ │删除选中行│ │ 清空表格 │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
      │
      │ + 边界条件（总风量、入/回风节点）
      │ + 求解器选项（迭代数、容差、松弛因子）
      │
      ▼
┌──────────────────┐
│ 求解网络         │ ← solve_network_from_ui.m
│ (Hardy Cross)    │
└────┬─────────────┘
      │
      ▼
┌──────────────────┐
│ 结果显示         │
│ - 风量           │
│ - 压降           │
│ - 收敛信息       │
└────┬─────────────┘
      │
      │ export_uitable_to_branches_csv.m
      ▼
┌──────────┐
│ CSV 文件 │
└──────────┘
```

---

## 🔧 在 App Designer 中配置

### 步骤 1：设置 UITable 属性

```matlab
function startupFcn(app)
    % 设置列可编辑性（ID 不可编辑，其他可编辑）
    app.UITable.ColumnEditable = [false, true, true, true];

    % 设置列名
    app.UITable.ColumnName = {'ID', '起点', '终点', '风阻'};

    % 设置选择类型（支持行选择）
    app.UITable.SelectionType = 'row';

    % 设置列宽
    app.UITable.ColumnWidth = {50, 80, 80, 100};
end
```

### 步骤 2：创建按钮并绑定回调

#### 导入按钮

```matlab
function ImportButtonPushed(app, event)
    T = gps.ui.import_branches_csv_to_uitable(app.UITable);
    if ~isempty(T)
        app.UITable.ColumnEditable = [false, true, true, true];
    end
end
```

#### 导出按钮

```matlab
function ExportButtonPushed(app, event)
    gps.ui.export_uitable_to_branches_csv(app.UITable);
end
```

#### 添加新行按钮

```matlab
function AddRowButtonPushed(app, event)
    gps.ui.add_new_row_to_uitable(app.UITable);
end
```

#### 删除选中行按钮

```matlab
function DeleteRowButtonPushed(app, event)
    gps.ui.delete_selected_rows_from_uitable(app.UITable, 'confirm', true);
end
```

#### 清空表格按钮

```matlab
function ClearButtonPushed(app, event)
    gps.ui.clear_uitable(app.UITable, 'confirm', true);
end
```

#### 求解按钮

```matlab
function SolveButtonPushed(app, event)
    [Q, Results, success] = gps.ui.solve_network_from_ui(app);
end
```

---

## 📋 推荐的 UI 布局

```
┌───────────────────────────────────────────────────────────────────┐
│  NetworkSolverApp                                                 │
├───────────────────────────────────────────────────────────────────┤
│  [导入]  [导出]  [添加新行]  [删除选中行]  [清空]  [求解]      │
├───────────────────────────────────────────────────────────────────┤
│  ┌──────────── 分支数据 ──────────┐  ┌──── 求解器设置 ────┐    │
│  │                                 │  │                     │    │
│  │    UITable (分支数据)           │  │ 初始风量: [100]     │    │
│  │  ┌────┬──────┬──────┬────────┐ │  │ 入风节点: [1]       │    │
│  │  │ ID │ 起点 │ 终点 │ 风阻   │ │  │ 回风节点: [10]      │    │
│  │  ├────┼──────┼──────┼────────┤ │  │                     │    │
│  │  │  1 │   1  │   2  │ 0.1    │ │  │ 最大迭代数: [1000]  │    │
│  │  │  2 │   2  │   3  │ 0.2    │ │  │ 收敛容差: [0.001]   │    │
│  │  │  3 │   3  │   4  │ 0.15   │ │  │ 松弛因子: [1.0]━━━  │    │
│  │  │ ...│  ... │  ... │ ...    │ │  │                     │    │
│  │  └────┴──────┴──────┴────────┘ │  │ 求解方法: [Hardy  ▼]│    │
│  │                                 │  │ 信息显示: [显示   ▼]│    │
│  └─────────────────────────────────┘  └─────────────────────┘    │
├───────────────────────────────────────────────────────────────────┤
│ 状态：共 8 行  |  求解状态：收敛 (迭代 45 次)                    │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🧪 测试验证

每个功能都提供了完整的测试脚本：

| 功能 | 测试脚本 | 运行命令 |
|-----|---------|---------|
| 导出功能 | `test_export.m` | `test_export` |
| 添加新行 | `test_add_row.m` | `test_add_row` |
| 删除选中行 | `test_delete_row.m` | `test_delete_row` |
| 清空表格 | `test_clear_uitable.m` | `test_clear_uitable` |

### 运行所有测试

```matlab
cd 'General Problem Solver/+gps/+ui'
test_export           % 测试导出功能
test_add_row          % 测试添加行功能
test_delete_row       % 测试删除行功能
test_clear_uitable    % 测试清空功能
```

---

## 📊 功能特性对比

| 功能 | 文件对话框 | 数据验证 | 焦点恢复 | 可选参数 |
|-----|-----------|---------|---------|---------|
| **导入** | ✅ | ✅ | ✅ | - |
| **导出** | ✅ | ✅ (10项) | ✅ | - |
| **添加行** | - | - | ✅ | - |
| **删除行** | - | - | - | ID重排(默认启用), 确认对话框 |
| **清空** | - | - | - | 确认对话框(默认启用) |

---

## 💡 高级技巧

### 技巧 1：组合使用多个功能

```matlab
function ProcessDataButtonPushed(app, event)
    % 1. 导入数据
    T = gps.ui.import_branches_csv_to_uitable(app.UITable);

    if isempty(T)
        return;
    end

    % 2. 自动添加一行
    gps.ui.add_new_row_to_uitable(app.UITable);

    % 3. 提示用户
    uialert(app.UIFigure, ...
        sprintf('已导入 %d 行数据并添加了 1 行空白行', height(T)), ...
        '完成');
end
```

### 技巧 2：快捷键绑定

```matlab
function UITableKeyPress(app, event)
    switch event.Key
        case 'delete'
            % Delete 键：删除选中行
            gps.ui.delete_selected_rows_from_uitable(app.UITable, 'confirm', true);

        case 'insert'
            % Insert 键：添加新行
            gps.ui.add_new_row_to_uitable(app.UITable);

        case 's'
            % Ctrl+S：导出数据
            if ismember('control', event.Modifier)
                gps.ui.export_uitable_to_branches_csv(app.UITable);
            end

        case 'd'
            % Ctrl+Shift+D：清空表格
            if ismember('control', event.Modifier) && ismember('shift', event.Modifier)
                gps.ui.clear_uitable(app.UITable, 'confirm', true);
            end
    end
end
```

### 技巧 3：自动保存功能

```matlab
properties (Access = private)
    AutoSaveTimer  % 定时器
end

function startupFcn(app)
    % 创建自动保存定时器（每 5 分钟）
    app.AutoSaveTimer = timer('ExecutionMode', 'fixedRate', ...
        'Period', 300, ...  % 5 分钟
        'TimerFcn', @(~,~) autoSave(app));

    start(app.AutoSaveTimer);
end

function autoSave(app)
    if ~isempty(app.UITable.Data)
        autoSaveFile = fullfile(tempdir, 'branches_autosave.csv');
        gps.ui.export_uitable_to_branches_csv(app.UITable, autoSaveFile);
        app.StatusLabel.Text = sprintf('自动保存于 %s', datestr(now, 'HH:MM:SS'));
    end
end
```

---

## ⚠️ 使用注意事项

### 1. 路径设置

确保在 `startupFcn` 中添加路径：

```matlab
function startupFcn(app)
    addpath('General Problem Solver');
end
```

### 2. 数据格式

所有函数都期望 UITable 数据为 `table` 类型，包含 4 列：
- 第 1 列：ID（整数）
- 第 2 列：起点（整数）
- 第 3 列：终点（整数）
- 第 4 列：风阻（正数）

### 3. 可编辑性

建议设置 ID 列不可编辑：

```matlab
app.UITable.ColumnEditable = [false, true, true, true];
```

### 4. 选择类型

删除功能需要表格支持行选择：

```matlab
app.UITable.SelectionType = 'row';  % 或 'cell'
```

---

## 🔗 相关模块

- **`+gps/+data/`** - 数据加载模块（从目录加载完整数据集）
- **`+gps/+logic/`** - 核心求解算法模块
- **`+gps/+ui/`** - UI 辅助功能模块（本模块）

---

## 📈 版本历史

- **v1.0** (2025-12-18) - 初始版本
  - 完整的 CRUD 功能（创建、读取、更新、删除）
  - CSV 导入/导出
  - 表格行操作（添加、删除、清空）
  - 通风网络求解器UI接口
  - 完整的测试覆盖

---

## 🎓 扩展建议

### 未来可能添加的功能

1. **撤销/重做** - 支持操作历史
2. **批量编辑** - 选中多行批量修改
3. **排序功能** - 按列排序表格
4. **过滤功能** - 根据条件筛选行
5. **复制/粘贴** - 与剪贴板交互
6. **拖拽排序** - 拖动行改变顺序
7. **数据验证** - 实时单元格验证
8. **历史记录** - 保存编辑历史

---

## 📞 技术支持

如有问题或建议，请查阅：
- 各功能的详细文档（README_*.md）
- 测试脚本（test_*.m）
- 函数内部注释

---

## 👤 作者

MATLAB 通风工程专家助手

## 📄 许可

遵循项目主许可证
